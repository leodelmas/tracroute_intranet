<form method="post" action="{{ path('app_admin_service_note_delete', {'id': service_note.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service_note.id) }}">
    <button class="btn">Delete</button>
</form>
