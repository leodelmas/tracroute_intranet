{{ form_start(form) }}
    <div class="row">
        <div class="col-sm-6">{{ form_row(form.title) }}</div>
        <div class="col-sm-6">{{ form_row(form.image) }}</div>
        <div class="col-sm-6">{{ form_row(form.start) }}</div>
        <div class="col-sm-6">{{ form_row(form.end) }}</div>
        <div class="col-sm-12">{{ form_row(form.content) }}</div>
    </div>
    <div class="d-flex justify-content-between">
        <a href="{{ path('app_admin_news_index') }}" class="btn btn-outline-secondary">{{ bootstrap_icon('arrow-left-circle') }} Actualités</a>
        <button class="btn btn-outline-primary">Enregistrer</button>
    </div>
{{ form_end(form) }}
