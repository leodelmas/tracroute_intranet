<form method="post" action="{{ path('app_admin_planned_slot_category_delete', {'id': planned_slot_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planned_slot_category.id) }}">
    <button class="btn btn-outline-danger">Supprimer {{ bootstrap_icon('trash') }}</button>
</form>
