{% extends 'base.html.twig' %}

{% block title %}HolidaysPeriod{% endblock %}

{% block body %}
    <h1>HolidaysPeriod</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ holidays_period.id }}</td>
            </tr>
            <tr>
                <th>Start</th>
                <td>{{ holidays_period.start ? holidays_period.start|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>End</th>
                <td>{{ holidays_period.end ? holidays_period.end|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ holidays_period.comment }}</td>
            </tr>
            <tr>
                <th>Approved</th>
                <td>{{ holidays_period.approved ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_holidays_period_index') }}">back to list</a>

    <a href="{{ path('app_holidays_period_edit', {'id': holidays_period.id}) }}">edit</a>

    {{ include('holidays_period/_delete_form.html.twig') }}
{% endblock %}
