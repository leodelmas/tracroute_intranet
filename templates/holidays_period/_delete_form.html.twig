<form method="post" action="{{ path('app_holidays_period_delete', {'id': holidays_period.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ holidays_period.id) }}">
    <button class="btn">Delete</button>
</form>
